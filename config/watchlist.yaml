# Tradeable names. Edit freely — the pipeline reads this file on every run.
# Growth pool 2026-08-22: SA Quant + growth factor, NVDA kept despite Quant Hold.
symbols:
  - MU     # HBM / DRAM
  - SNDK   # NAND
  - AMD    # GPU
  - NVDA   # AI compute (Quant Hold, Growth A+)
  - AMZN   # cloud (only Mag7 Quant Strong Buy)
  - ANET   # networking
  - CRDO   # SerDes / AEC
  - ALAB   # PCIe / CXL
  - NBIS   # AI cloud
  - HPE    # servers
  - FLEX   # EMS
  - CIEN   # optical
  - ARGX   # biotech (non-tech)
  - VEEV   # life-science software (non-tech)

# Regime / context only — scored for macro, never bought. Put a name in
# `symbols` instead if you actually want the agent to trade the ETF.
context_symbols:
  - SPY
  - QQQ
