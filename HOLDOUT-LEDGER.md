# Holdout 消耗台账

**规则**：一段数据一旦被用来比较参数并影响选择，它对"调参决策"而言就不再是
样本外。此后每次再看都是变相 in-sample。本文件记录哪些窗口已被消耗、被什么
消耗、还剩多少预算。

---

## 已消耗窗口

### 2023-01-03 → 2026-08-21（train 2019–2022 / holdout 切分）

| 项目 | 内容 |
|---|---|
| 消耗时间 | 2026-08-22（Grok 回测会话，见 `HANDOFF-BACKTEST.md` §5.3） |
| 被检验的决策 | `buy_threshold` 三选一：0.20 / 0.25 / 0.35（按 IS Sharpe 选优后到 holdout 上验证） |
| holdout 结论 | **0.25 全面胜出 0.35**（CAGR +26.9% vs +22.9%，Sharpe 1.54 vs 1.35）；SPY 同期 Sharpe 1.44 |
| 决策后果 | **未改 live 参数**——0.35 保留为风险选择而非 OOS 最优（当时明确记录） |
| 状态 | 🔴 **已消耗** |

**含义**：

1. 任何针对该窗口的回测数字（包括 audit 的 `current_holdout_2023_2026` spec、
   `--walk-forward` 覆盖 2023+ 的 fold）在调参语境下都按 **in-sample** 对待。
2. 允许的剩余用途：**一次** 预注册式确认——只有当你带着一个事先写下的、唯一
   的候选改动来验证时才可再看；看完即二次消耗，窗口彻底封存。
3. 新的样本外数据从 2026-08-21 之后自然累积。诚实做法是等真实时间走出新窗口，
   而不是反复切旧窗口。
4. 幸存者池问题独立存在：即使换参数，12 只旧书本身是事后赢家。参数层 OOS 救
   不了宇宙层 IS——这也是 `research/ACTIVE-BOOK-VALIDATION-PLAN.md` 存在的原因。

## 新增消耗时的登记格式

```
### <窗口起> → <窗口止>
- 消耗时间：
- 被检验的决策：（必须只有一个）
- 结论与是否采纳：
- 状态：<🟡 剩一次确认 | 🔴 封存>
```
